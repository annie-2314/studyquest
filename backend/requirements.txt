fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
alembic==1.13.2
pydantic==2.9.2
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.9
pytest==8.3.3
httpx==0.27.2
email-validator==2.2.0
# --- Phase 2: agents (OpenRouter via OpenAI-compatible API) ---
langgraph>=0.2.20,<0.3
langchain-core>=0.3.10,<0.4
langchain-openai>=0.2.2,<0.3
truststore==0.9.2  # OS cert store for the httpx/LLM client (TLS-inspecting proxies)
pip-system-certs==4.0  # makes urllib/requests (yt-dlp, transcripts) use the OS cert store too
# --- Phase 3: basic RAG (offline, no vector server) ---
rank-bm25==0.2.2
# --- Phase 4: YouTube ingestion (no API key required) ---
yt-dlp==2024.8.6
youtube-transcript-api==0.6.2
# --- Phase 6: OPTIONAL file transcription (heavy; YouTube-URL path works without it) ---
# faster-whisper==1.0.3
# --- Phase 8: PDF study guide ---
fpdf2==2.7.9
# CrewAI (OPTIONAL): requires MS C++ Build Tools (chromadb/hnswlib) on Windows.
# The Study-Plan crew runs without it via app/crew/study_plan.py.
# crewai==0.51.1
